# ── oss-scout 配置 ──
# 复制本文件为 .env，然后在 .env 里填值（.env 已被 gitignore，不会误提交）：
#   cp .env.example .env

# Anthropic API key —— analyze / propose / brief / draft 需要
# 必须是标准的 sk-ant-api... key（在 platform.claude.com 申请）。
# 注意：若 shell 里已有一个非标准的网关 ANTHROPIC_API_KEY，config.py 会用本文件的值覆盖它。
ANTHROPIC_API_KEY=

# GitHub token（可选）：把 GitHub API 限额从 60 提到 5000 次/小时
GITHUB_TOKEN=

# 使用的 Claude 模型，默认 claude-opus-4-8
# OSS_SCOUT_MODEL=claude-opus-4-8
